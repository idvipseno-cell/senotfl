# دليل تشغيل نظام بوتات التفاعل التلقائي 🚀

هذا النظام مصمم لزيادة التفاعل في قنوات التلغرام تلقائياً باستخدام بوت رئيسي ومجموعة من البوتات الثانوية (حتى 20 بوت أو أكثر).

## 📌 المميزات:
- **تفاعل تلقائي:** البوتات تتفاعل فوراً مع أي منشور جديد.
- **تعدد البوتات:** يمكنك إضافة حتى 20 بوت ثانوياً لزيادة عدد التفاعلات لكل رسالة.
- **الاشتراك الإجباري:** لا يمكن للمستخدم تفعيل الخدمة إلا بعد الاشتراك في قناتك.
- **التعطيل التلقائي:** إذا غادر المستخدم القناة الإجبارية، يتم تعطيل التفاعل في قناته فوراً.
- **لوحة تحكم:** إذاعة للمستخدمين وإدارة كاملة.

---

## 🛠 متطلبات التشغيل:
1. **بايثون (Python 3.9+).**
2. **المكتبات المطلوبة:** `aiogram`.
3. **توكنات البوتات:** (توكن للبوت الرئيسي + توكنات للبوتات الثانوية من @BotFather).

---

## 🚀 خطوات التثبيت:

### 1. تجهيز البيئة:
قم بتثبيت المكتبات اللازمة باستخدام الأمر التالي:
```bash
pip install aiogram
```

### 2. الإعدادات (config.py):
افتح ملف `config.py` وقم بتعديل القيم التالية:
- `MAIN_BOT_TOKEN`: توكن البوت الرئيسي.
- `ADMIN_IDS`: معرفك (ID) لتتمكن من استخدام لوحة الآدمن.
- `REQUIRED_CHANNEL`: يوزر قناتك للاشتراك الإجباري.

### 3. إضافة البوتات الثانوية:
لإضافة البوتات التي ستقوم بالتفاعل، يجب إضافتها إلى قاعدة البيانات. يمكنك القيام بذلك عبر لوحة الآدمن في البوت الرئيسي أو يدوياً في ملف `database/bot_data.db`.

### 4. التشغيل:
يجب تشغيل ملفين في نفس الوقت:
1. **تشغيل البوت الرئيسي:**
   ```bash
   python bot_main/main.py
   ```
2. **تشغيل نظام التفاعل (البوتات الثانوية):**
   ```bash
   python bot_secondary/multi_worker.py
   ```

---

## 📝 ملاحظات هامة:
- يجب رفع **جميع البوتات** (الرئيسي والثانوية) كمشرفين في القناة المراد التفاعل فيها.
- تأكد من أن القناة **عامة** وليست خاصة.
- البوتات الثانوية تحتاج لصلاحية "نشر الرسائل" أو "تعديل الرسائل" (حسب نوع التفاعل) لتعمل بشكل صحيح.

---
**تم التطوير بواسطة Manus AI** 🤖
