# دليل تشغيل نظام بوتات التفاعل التلقائي 🚀

هذا النظام مصمم لزيادة التفاعل في قنوات التلغرام تلقائياً باستخدام بوت رئيسي ومجموعة من البوتات الثانوية.

## 🚀 كيفية النشر على Railway:

منصة Railway هي أسهل طريقة لتشغيل البوت الخاص بك على مدار الساعة 24/7. اتبع الخطوات التالية:

### 1. تجهيز المشروع:
- قم برفع ملفات المشروع إلى مستودع (Repository) خاص بك على **GitHub**.

### 2. الربط مع Railway:
- اذهب إلى موقع [Railway.app](https://railway.app/) وسجل دخولك باستخدام حساب GitHub.
- اضغط على **"New Project"** ثم اختر **"Deploy from GitHub repo"**.
- اختر المستودع الذي رفعت فيه ملفات البوت.

### 3. إضافة متغيرات البيئة (Variables):
في واجهة مشروعك على Railway، انتقل إلى تبويب **Variables** وأضف القيم التالية:
- `MAIN_BOT_TOKEN`: توكن البوت الرئيسي الخاص بك.
- `ADMIN_IDS`: معرفك (ID) في تلغرام (يمكنك إضافة عدة معرفات مفصولة بفاصلة).
- `REQUIRED_CHANNEL`: يوزر القناة الإجبارية (مثال: `@MyChannel`).

### 4. التشغيل:
- سيقوم Railway تلقائياً بالتعرف على ملف `Procfile` وتشغيل البوت الرئيسي ونظام التفاعل.
- يمكنك مراقبة السجلات (Logs) للتأكد من أن كل شيء يعمل بشكل صحيح.

---

## 💻 التشغيل المحلي (على جهازك):

1. **تثبيت المكتبات:**
   ```bash
   pip install -r requirements.txt
   ```
2. **تعديل الإعدادات:**
   افتح `config.py` وضع التوكنات الخاصة بك يدوياً أو قم بضبط متغيرات البيئة في جهازك.
3. **بدء التشغيل:**
   - افتح نافذتين للطرفية (Terminal).
   - في الأولى شغل البوت الرئيسي: `python bot_main/main.py`
   - في الثانية شغل نظام التفاعل: `python bot_secondary/multi_worker.py`

---

## 📝 ملاحظات هامة:
- تأكد من إضافة البوت الرئيسي والثانوية كمشرفين في القنوات.
- النظام يستخدم قاعدة بيانات SQLite محلية، عند النشر على Railway يفضل استخدام خدمة قاعدة بيانات خارجية إذا كنت تخطط لتغيير الملفات باستمرار، ولكن للبدء، النسخة الحالية تعمل بشكل ممتاز.

---
**تم التطوير بواسطة Manus AI** 🤖
